# Package

version       = "0.1.0"
author        = "jaar23"
description   = "simple message queue system"
license       = "MIT"
srcDir        = "src"
bin           = @["octoque"]


# Dependencies

requires "nim >= 2.0.0"
requires "uuid4 >= 0.9.3"
requires "octolog >= 0.1.0"
requires "argparse >= 4.0.1"
requires "nimword >= 1.0.0"
